// import { useEffect } from "react"

function App() {
   // useEffect(() => {
   //    const tickets = async () => {
   //       const response = await fetch("/api/tickets");
   //       const data = await response.json();

   //       console.log(data);

   //    }

   //    tickets();
   // }, [])
  return (
   <></>
  )
}

export default App
